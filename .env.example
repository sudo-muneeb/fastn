FASTN_API_KEY=blockrealm-fastn-key
MOCK_API_KEY=blockrealm-mock-key
PORT=3000
# optional
# DATA_FILE=/data/db.json   (persist on a Railway volume)
# MOCK_UNIQUE=true
# SEED_FILE=none            (start empty instead of loading backend/seed/db.json)
