{
  "name": "blockrealm-insights-backend",
  "version": "1.0.0",
  "description": "BlockRealm Insights backend: mock Reddit/X APIs, Fastn ingest/export API and dashboard analytics",
  "type": "module",
  "main": "src/server.js",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "NODE_ENV=development node --watch src/server.js",
    "test": "NODE_ENV=test node --test \"tests/*.test.js\"",
    "simulate": "node scripts/simulate-fastn.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
